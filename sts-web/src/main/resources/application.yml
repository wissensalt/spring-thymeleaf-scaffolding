server:
  port : 8080
  tomcat:
    apr:
      enabled: true

spring:
#  mvc:
#    static-path-pattern: /resources/**
  thymeleaf:
    cache: false
    mode: HTML
    templates_root: sts-web/src/main/resources/templates/
  devtools:
    restart:
      enabled : true

logging:
  level:
    #    web : DEBUG
    com:
      wissensalt:
        rnd:
          slr:
            web:
              feign: DEBUG

api:
  base-path : http://localhost:8081

web :
  head :
    title : Spring
    subtitle : Thymeleaf Scaffolding
  footer :
    version : 1.0.0
    year-range : 2019-2020
    link : https://wissensalt.wordpress.com/
    link-text : Wissensalt